"use client";import { Bar,BarChart,CartesianGrid,Cell,Line,LineChart,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";import type { AnalyticsPoint,TrafficSourceAnalytics } from "@/types/lark";
const tip={contentStyle:{borderRadius:10,border:"1px solid #e5e7eb",boxShadow:"0 8px 24px rgba(15,23,42,.08)",fontSize:12}};
export function TrendChart({data,metric="views",bar=false}:{data:AnalyticsPoint[];metric?:keyof AnalyticsPoint;bar?:boolean}){return <ResponsiveContainer width="100%" height={290}>{bar?<BarChart data={data}><CartesianGrid vertical={false} stroke="#eef2f7"/><XAxis dataKey="label" tickLine={false} axisLine={false}/><YAxis tickLine={false} axisLine={false}/><Tooltip {...tip}/><Bar dataKey={metric} fill="#16a34a" radius={[5,5,0,0]}/></BarChart>:<LineChart data={data}><CartesianGrid vertical={false} stroke="#eef2f7"/><XAxis dataKey="label" tickLine={false} axisLine={false}/><YAxis tickLine={false} axisLine={false}/><Tooltip {...tip}/><Line type="monotone" dataKey={metric} stroke="#16a34a" strokeWidth={2.5} dot={false} activeDot={{r:4}}/></LineChart>}</ResponsiveContainer>}
export function TrafficDonut({data}:{data:TrafficSourceAnalytics[]}){const colors=["#16a34a","#4ade80","#86efac","#bbf7d0","#dcfce7"];return <ResponsiveContainer width="100%" height={300}><PieChart><Pie data={data} dataKey="value" nameKey="label" innerRadius={72} outerRadius={112} paddingAngle={2}>{data.map((x,i)=><Cell key={x.key} fill={colors[i]}/>)}</Pie><Tooltip {...tip}/></PieChart></ResponsiveContainer>}
