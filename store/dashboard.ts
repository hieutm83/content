import { create } from "zustand";export type Range="yesterday"|"7d"|"30d"|"90d"|"custom";interface State{range:Range;start?:string;end?:string;week:string;month:string;setRange:(x:Range)=>void;setDates:(start:string,end:string)=>void;setWeek:(x:string)=>void;setMonth:(x:string)=>void}export const useDashboardStore=create<State>(set=>({range:"yesterday",week:"all",month:"all",setRange:range=>set({range}),setDates:(start,end)=>set({start,end,range:"custom"}),setWeek:week=>set({week}),setMonth:month=>set({month})}));
